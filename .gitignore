# Local AI tooling — not part of the storefront.
# These are generated or machine-local and should never ship with the site.
.codegraph/
.atl/
.agents/
.claude/
skills-lock.json

# OS noise
.DS_Store
Thumbs.db

# Astro (migración) - por si acaso, aunque astro-app/ trae su propio .gitignore
astro-app/node_modules/
astro-app/dist/
astro-app/.astro/
astro-app/.env
